# 필지종합조회 시스템 기획서

> 상태: 초안 (v0.1)
> 미확정 항목은 본문에 `TBD`로 표기하고, 11장 「미결정 사항」에 모아두었습니다.

---

## 1. 개요

주소 하나를 입력하면 해당 필지의 **토지대장 / 건축물대장 / 공시지가 / 토지이용계획** 4종 정보를 한 화면에서 열람할 수 있는 웹 서비스.

여러 기관 사이트를 각각 방문해야 얻을 수 있는 필지 정보를 단일 진입점으로 통합하는 것이 목적이다.

---

## 2. 범위

### 포함

- 주소 검색 및 필지 선택
- 4종 정보 + 요약을 보고서 형태로 한 화면에 순서대로 열람 (pills는 각 섹션으로 이동하는 앵커 내비게이션)
- 여러 필지를 브라우저 탭처럼 동시에 열어두고 전환·닫기 (5장 참조)
- 공시지가 5개년 추이 차트 (지도와 함께 배치)

### 제외

| 항목 | 사유 |
| --- | --- |
| 최근 조회 이력 | 범위에서 제외 |
| 즐겨찾기 · 서버 저장 이력 | 로그인 미도입에 따라 제외 |
| 필지 검색·탐색용 지도(지도를 클릭해 필지 찾기 등) | 요구사항에 없음 — F-04의 위치 표시용 지도와는 별개 |
| 출력 · 다운로드(PDF 등) | 요구사항에 없음 |
| 응답 캐싱(Redis 등) | 범위에서 제외 — 매 조회마다 공공 API 직접 호출 |

---

## 3. 사용자 시나리오

1. 사용자가 검색창에 주소를 입력하고 검색 버튼을 누른다.
2. 후보 주소 목록이 표시되고, 사용자가 그중 하나를 선택한다.
3. 선택한 필지가 상단에 탭으로 열리고, 그 아래 5개의 pills(앵커 내비게이션)가 나타난다.
4. 그 아래로 요약 · 토지이용계획 · 토지대장 · 건축물대장 · 공시지가 5개 섹션이 보고서 형태로 순서대로 이어진다.
5. 사용자가 pills 중 하나를 누르면 해당 섹션으로 스크롤 이동한다. 스크롤을 직접 내려도 지나는 섹션에 맞춰 활성 pill이 바뀐다.
6. 검색창 왼쪽 "+"를 누르면 새 주소를 검색해 별도 탭으로 추가할 수 있다. 여러 필지를 동시에 열어두고 탭을 눌러 전환하거나 ×로 닫을 수 있다.

---

## 4. 기능 요구사항

### F-00 요약

- 4종 정보(토지이용계획·토지대장·건축물대장·공시지가)의 핵심 값만 4카드 그리드로 한눈에 보여준다.
- 각 카드는 해당 섹션이 아직 로딩 중이면 회색조로, 완료되면 실제 값으로 표시된다.

### F-01 주소 검색

- 입력: 사용자가 타이핑한 주소 문자열
- 동작: 검색 버튼 클릭(또는 Enter) 시 후보 목록 조회 → 목록에서 선택. 입력하는 대로 실시간으로 후보가 뜨는 자동완성은 아니다.
- 결과 없음 / 다수 결과 / 오류 상태를 각각 구분해 표시
- 여러 필지를 동시에 열어둘 수 있다 — 이미 연 필지는 탭으로 남아 있고, "+"로 새 검색을 추가한다 (2·3장 참조)

### F-02 토지대장 열람

표시 항목: **지목, 면적, 소유구분, 이용상황**

### F-03 건축물대장 열람

표시 항목: **주구조, 주용도, 대지면적·건축면적, 건폐율·용적률, 사용승인일, 층별 개요**

- 층별 개요는 층 단위 목록 형태로 표시
- 건폐율·용적률은 건축물대장 API에서 얻는다 (11장 참조 — 토지이용계획 쪽 API엔 없음이 확인됨)
- 해당 필지에 건축물이 없는 경우 "건축물 없음" 상태를 명시

### F-04 공시지가 트래커

- 5개년 개별공시지가를 **연도별 꺾은선 차트**로 표시 (조회 시점 기준 최근 5년)
- 차트와 함께 연도·금액 원본 값을 표 형태로 병기
- 일부 연도 데이터 누락 시 해당 연도는 선을 끊어 표시
- 화면 왼쪽에는 해당 주소의 지도 위치, 오른쪽에는 위 차트를 배치 (5장 참조)

### F-05 토지이용계획 조회

표시 항목: **용도지역, 규제**

- 규제는 항목 수가 가변적이므로 목록 형태로 표시, 저촉/해당/접함 구분 뱃지를 붙인다
- 건폐율·용적률은 이 섹션이 아니라 F-03(건축물대장)에서 보여준다

---

## 5. 화면 설계

단일 페이지, **보고서 형태**로 5개 섹션을 한 화면에 이어서 보여준다.
pills는 콘텐츠를 전환하는 탭이 아니라, 아래 섹션으로 스크롤 이동시키는 **앵커 내비게이션**이다. (열어둔 필지들을 전환하는 상단 탭바와는 별개다 — 아래 참조)

섹션 노출 순서: **요약 → 토지이용계획 → 토지대장 → 건축물대장 → 공시지가 트래커**

```
┌─────────────────────────────────────────────────┐
│ [강남구 테헤란로 152 ×] [강남구 언주로85길 21 ×] [+]│  ← 열린 필지 탭바 (여러 개 동시)
├─────────────────────────────────────────────────┤
│  [ 주소 입력                     ]  [ 검색 ]     │
├─────────────────────────────────────────────────┤
│  (검색 후) 후보 주소 목록 — 선택                 │
├─────────────────────────────────────────────────┤
│  선택된 필지 주소                                │
├─────────────────────────────────────────────────┤
│ (요약) (토지이용계획) (토지대장) (건축물대장) (공시지가) │  ← pills = 앵커 내비게이션
├─────────────────────────────────────────────────┤
│  ▣ 요약 섹션 — 4카드 그리드                      │
├─────────────────────────────────────────────────┤
│  ▣ 토지이용계획 조회 섹션                        │
│    용도지역 · 규제(저촉/해당/접함 뱃지)           │
├─────────────────────────────────────────────────┤
│  ▣ 토지대장 섹션                                 │
│    지목 · 면적 · 소유구분 · 이용상황              │
├─────────────────────────────────────────────────┤
│  ▣ 건축물대장 섹션                                │
│    주구조 · 주용도 · 면적 · 건폐율/용적률 · 사용승인일 · 층별개요 │
├─────────────────────────────────────────────────┤
│  ▣ 공시지가 트래커 섹션                           │
│  ┌───────────────┬───────────────────────────┐  │
│  │   지도 (좌)    │      5개년 꺾은선 차트 (우)  │  │
│  │ 해당 주소 위치 │      + 연도·금액 표         │  │
│  └───────────────┴───────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

### 필지 탭바

- 검색해서 선택한 필지는 브라우저 탭처럼 상단에 열린 채로 남는다. 탭을 누르면 해당 필지로 전환, ×로 닫는다.
- 검색창 왼쪽 "+"를 누르면 새 검색을 시작해 다른 필지를 추가로 열 수 있다.
- 탭에는 도로명주소(축약)와 조회 상태 요약(로딩 중이면 "조회 중…", 완료되면 용도지역·공시지가 요약)을 표시한다.
- 탭별로 5개 섹션의 로딩 상태·데이터를 독립적으로 유지한다 (탭 전환해도 다시 로딩하지 않음).

### pills(앵커 내비게이션) 동작 규칙

- 클릭 시 해당 섹션으로 스크롤 이동 (콘텐츠를 숨기거나 교체하지 않음)
- 스크롤 위치에 따라 현재 보고 있는 섹션의 pill이 활성 표시로 강조됨
- 5개 섹션은 필지 선택 시 **함께 조회를 시작** — 각 섹션이 서로 기다리지 않고 독립적으로 로딩 상태를 표시

### 섹션 상태

각 섹션은 아래 4가지 상태를 독립적으로 가진다. 한 섹션의 오류/데이터 없음이 다른 섹션에 영향을 주지 않는다.

| 상태 | 표시 |
| --- | --- |
| 로딩 | 스켈레톤 |
| 정상 | 항목별 데이터 |
| 데이터 없음 | 안내 문구 (예: 해당 필지에 건축물 없음) |
| 오류 | 오류 안내 + 재시도 버튼 |

### 공시지가 트래커 섹션 레이아웃

- 좌: 선택된 주소 위치를 표시하는 지도 UI
- 우: 5개년 개별공시지가 꺾은선 차트 + 연도·금액 표
- 좌표는 F-01에서 선택한 주소 문자열을 8-1의 Geocoder API(`getCoord`)에 그대로 넣어 얻는다 — 확인됨. `pnu`는 필요 없다.

---

## 6. 기술 스택

| 구분 | 기술 |
| --- | --- |
| 프론트엔드 | React + Vite |
| 백엔드 | FastAPI (Python) |
| 캐시 | 없음 |
| 차트 | Recharts |
| 지도 | Leaflet `react-leaflet` |
| 영속 DB | 없음 |

---

## 7. 시스템 아키텍처

```
[ 브라우저 (React + Vite) ]
            │  자체 REST API
            ▼
[ FastAPI 백엔드 ]
            │
            └──▶ [ 공공 API 6종 ]
```

### 백엔드를 경유하는 이유

- **API 인증키 보호** — 프론트에서 공공 API를 직접 호출하면 키가 노출됨
- **응답 형태 정규화** — 기관별로 응답 구조가 제각각이므로 프론트가 쓰기 좋은 형태로 백엔드에서 변환
- **CORS 회피**

### 백엔드 레이어 구성

| 레이어 | 역할 |
| --- | --- |
| Router | 자체 REST 엔드포인트 정의, 요청 검증 |
| Service | 조회 흐름 조합 |
| Client | 공공 API별 어댑터 (호출 + 응답 파싱) |
| Schema | 응답 모델 (Pydantic) |

공공 API 하나당 Client 모듈 하나를 두어, 특정 API 명세가 바뀌어도 해당 모듈만 수정하면 되도록 격리한다.

---

## 8. 외부 API 연동 계획

> 아래는 각 API 문서 페이지를 Chrome DevTools로 직접 렌더링해 확인한 내용이다(2026-07-27 기준, 실제 발급키로 호출 테스트는 아직 안 한 상태).

| 기능 | 사용 API | 확인 상태 |
| --- | --- | --- |
| F-01 주소 검색 | 도로명주소 API (juso.go.kr) | ✅ 확인 |
| F-02 토지대장 | 토지·임야정보 API (VWorld) | ✅ 확인 |
| F-03 건축물대장 | 건축물대장정보 API (data.go.kr, 건축HUB) | ✅ 확인 |
| F-04 공시지가 | 개별공시지가 API (VWorld) | ✅ 확인 (일부) |
| F-05 토지이용계획 | 토지이용계획 API (VWorld, `getLandUseAttr`) | ⚠️ 확인 — 용도지역·저촉여부만 제공, 건폐율·용적률·규제는 이 API에 없음(확인됨) |
| F-04 지도(좌표 변환) | Geocoder API (VWorld) | ✅ 확인 |

### 8-1. 확인된 내용

**도로명주소 API** — `https://business.juso.go.kr/addrlink/addrLinkApi.do` (GET/POST, xml/json), 크로스도메인용 JSONP: `addrLinkApiJsonp.do`
- 파라미터: `confmKey`(승인키, 필수), `currentPage`/`countPerPage`(페이징, 필수), `keyword`(검색어, 필수), `resultType`(xml/json), `hstryYn`/`firstSort`/`addInfoYn`(부가 옵션)
- 응답 필드(주요): `roadAddr`/`jibunAddr`(도로명/지번주소), `zipNo`, `siNm`/`sggNm`/`emdNm`/`liNm`(시도/시군구/읍면동/법정리), `admCd`(행정구역코드), `rnMgtSn`(도로명코드), `bdMgtSn`(건물관리번호), `buldMnnm`/`buldSlno`(건물본번/부번), `mtYn`(산여부 0:대지·1:산), `lnbrMnnm`(지번본번), `lnbrSlno`(사이트 설명은 "읍면동일련번호"라고 되어 있으나, `buldSlno`와의 명명 규칙상 지번부번을 뜻하는 것으로 보임 — 실제 응답값으로 재확인 필요)
- **PNU 자체는 없지만, `admCd`(행정구역코드) + `mtYn`(산여부) + `lnbrMnnm`(본번) + `lnbrSlno`(부번)를 조합하면 F-02·F-05가 쓰는 `pnu`(19자리: 법정동코드10 + 산여부1 + 본번4 + 부번4)를 구성할 수 있는 구조로 보인다.** 다만 `admCd`가 정확히 10자리 법정동코드와 동일한지는 문서에 명시돼 있지 않아 실제 호출 결과로 확인이 필요하다.
- 인증키(`confmKey`)는 8-1 하단 "인증키 통합" 항목대로 `VWORLD_API_KEY`를 사용하기로 확정됨

**건축물대장정보 API (건축HUB)** — Base URL `apis.data.go.kr/1613000/BldRgstHubService`, 인증은 data.go.kr 표준 `serviceKey` (VWorld와 무관 — `BUILDING_API_KEY`로 분리한 게 맞았음)
- **식별 파라미터가 `pnu`가 아니다.** `sigunguCd`(시군구코드, 필수) + `bjdongCd`(법정동코드, 필수) + `platGbCd`(대지구분코드: 0:대지·1:산·2:블록, 선택) + `bun`(번, 선택) + `ji`(지, 선택) 조합으로 조회한다. 도로명주소 API의 `admCd`를 앞 5자리(`sigunguCd`)/뒤 5자리(`bjdongCd`)로 나누고, `mtYn`→`platGbCd`, `lnbrMnnm`→`bun`, `lnbrSlno`→`ji`로 매핑하면 될 것으로 보이나, `platGbCd`의 "2:블록" 값은 `mtYn`에 없는 값이라 완전히 1:1은 아니다.
- 여러 오퍼레이션(표제부·기본개요·총괄표제부·층별개요·부속지번·전유공용면적·오수정화시설·주택가격·전유부·지역지구구역) 중 F-03에 필요한 건 아래 2개:
  - **`getBrTitleInfo`(표제부 조회)**: 응답에 `strctCdNm`(**주구조**), `mainPurpsCdNm`(**용도**), `useAprDay`(**사용승인일**) 포함. 그 외 `platArea`(대지면적), `archArea`(건축면적), `bcRat`(건폐율), `totArea`(연면적), `vlRat`(용적률), `mgmBldrgstPk`(건축물대장 PK) 등도 함께 내려옴
  - **`getBrFlrOulnInfo`(층별개요 조회)**: 요청 파라미터는 표제부와 동일. 응답이 층 단위 리스트(`item`)이며 각 항목에 `flrGbCdNm`(지상/지하 구분), `flrNoNm`(층번호), `strctCdNm`(층별 구조), `mainPurpsCdNm`(층별 용도), `area`(층별 면적) 포함 — **F-03의 "층별 개요" 요구사항과 정확히 일치**
- 해당 필지에 건축물이 없으면 `totalCount: 0`으로 응답할 것으로 보임(다른 data.go.kr 계열 API와 동일한 패턴) — 실제 호출로 확인 필요

**토지임야목록조회 API** — `https://api.vworld.kr/ned/data/ladfrlList`
- 식별 파라미터: `pnu` (고유번호, 필수), `key` (인증키, 필수)
- 옵션: `format`(xml/json), `numOfRows`, `pageNo`, `domain`
- 응답 필드: 지목 `lndcgrCode`/`lndcgrCodeNm`, 면적 `lndpclAr`, 소유구분 `posesnSeCode`/`posesnSeCodeNm`
- **이용상황에 해당하는 필드는 문서에서 확인 안 됨** — 추가 확인 필요

**개별공시지가 API** — `https://api.vworld.kr/ned/data/getIndvdLandPrice`
- 파라미터: `key`(필수), `stdrYear`(기준연도, 옵션), `ldCode`(법정동코드, 옵션), `reqLvl`(시도/시군구/읍면동 단위), `format`
- 응답 필드: 공시지가 `ladPblntfPclnd`(원/㎡), 기준연도 `stdrYear`
- **주의**: 식별자가 `pnu`가 아니라 `ldCode`(법정동코드) — 특정 필지 단위로 좁히는 파라미터가 문서 요약에는 보이지 않음. 지번까지 특정해서 조회되는지 확인 필요
- **5개년 다년치를 1회 호출로 받을 수 있는지 불명** — `stdrYear`는 단일 연도로 보임 (연도별 5회 호출 가능성)

**토지이용계획 API** — `https://api.vworld.kr/ned/data/getLandUseAttr` (2026-07-27 Chrome DevTools로 실제 레퍼런스 페이지 재확인)
- 식별 파라미터: `pnu` (고유번호, 필수), `key` (인증키, 필수) — 토지·임야정보 API와 **동일하게 `pnu` 기반**. 응답 샘플의 `pnu`가 19자리(`1165010800113320002`)인 것도 확인 — 8-1 도로명주소 항목의 "법정동코드10+산여부1+본번4+부번4=19자리" 추정을 뒷받침함
- 옵션: `cnflcAt`(저촉여부코드: 1 포함/2 저촉/3 접합), `prposAreaDstrcCodeNm`(용도지역지구명), `format`, `numOfRows`, `pageNo`, `domain`
- **응답 필드 전체(레퍼런스 페이지에 명시된 전부)**: `pnu`(고유번호), `ldCode`(법정동코드), `ldCodeNm`(법정동명), `regstrSeCode`/`regstrSeCodeNm`(대장구분코드/명, 예: "토지대장"), `mnnmSlno`(지번), `manageNo`(도면번호), `cnflcAt`/`cnflcAtNm`(저촉여부코드/명), `prposAreaDstrcCode`/`prposAreaDstrcCodeNm`(용도지역지구코드/명), `registDt`(등록일자), `lastUpdtDt`(데이터기준일자)
- **건폐율·용적률·규제사항 필드는 이 API에 없다는 것이 확정됨.** 응답 항목이 위 10개가 전부이며, 페이지 어디에도 건폐율·용적률에 해당하는 필드가 없다. F-05를 채우려면 **다른 API나 데이터 출처가 필요** — VWorld의 "국가중점데이터 컬럼정의서" 다운로드나 별도 서비스(예: 토지이용규제정보서비스 luris.molit.go.kr)를 확인해야 한다
- 일일 호출 제한 "999,999,999" 재확인됨(오퍼레이션 페이지에 "제공가능 트래픽"으로 명시) — 사실상 무제한에 가까운 기본 할당치로 보임
- 오류 코드 체계 확인: `PARAM_REQUIRED`, `INVALID_TYPE`, `INVALID_RANGE`, `INVALID_KEY`, `INCORRECT_KEY`, `UNAVAILABLE_KEY`, `OVER_REQUEST_LIMIT`, `SYSTEM_ERROR`, `UNKNOWN_ERROR` — VWorld 계열 API 공통으로 보이며, 9장 "공공 API 오류를 서비스 표준 오류로 변환" 시 이 코드를 매핑 기준으로 쓸 수 있음

**Geocoder API (주소→좌표)** — `https://api.vworld.kr/req/address?service=address&request=getCoord`
- 파라미터: `request=GetCoord`, `type`(PARCEL/ROAD), `address`(주소 문자열), `key`(필수)
- 응답: `result.point.x`, `result.point.y` (기본 좌표계 EPSG:4326)
- 일일 호출 한도 40,000건
- F-04 지도 UI의 좌표는 **선택된 주소 문자열을 이 API에 그대로 넣어 얻는다** — PNU 불필요, F-01에서 선택한 주소로 바로 호출 가능

**인증키 통합 (확정, 2026-07-27)** — 도로명주소·토지·임야정보·토지이용계획·개별공시지가·Geocoder 5개 API는 `VWORLD_API_KEY` 하나로 통합해서 쓴다. 건축물대장만 별도로 `BUILDING_API_KEY`(data.go.kr 표준 `serviceKey`)를 쓴다 (`backend/.env.example` 참조).

### 8-2. 남은 핵심 확인 사항

이제 6개 API 모두 문서 확인은 끝났다. 남은 건 실제 호출 테스트로만 확인 가능한 것들이다.

1. **도로명주소 `admCd`가 정확히 10자리 법정동코드로 나뉘는지** — 8-1 참조. F-02·F-03·F-05를 도로명주소 검색 결과만으로 바로 호출할 수 있는지가 여기 달려 있음
2. **F-05 건폐율·용적률·규제사항을 어디서 가져올지** — `getLandUseAttr`에는 없는 것으로 확정됨(8-1 참조). 국가중점데이터 컬럼정의서 확인 또는 토지이용규제정보서비스(luris.molit.go.kr) 등 별도 API 조사가 필요
3. F-04 개별공시지가가 필지 단위로 조회되는지, 5개년을 1회/5회 중 어느 쪽으로 받는지
4. 건축물대장 `platGbCd`의 "2:블록" 값이 도로명주소 `mtYn`(0/1만 있음)으로는 안 나옴 — 블록형 대지가 실제로 몇 건이나 있는지, 어떻게 처리할지

특히 **1번은 전체 아키텍처의 전제**이므로, 확인 전까지는 9장 이후의 설계를 완전히 확정하지 않는다 (다만 1번이 대체로 맞을 가능성이 높다고 보고 9장은 이를 전제로 초안을 작성해뒀다).

---

## 9. 백엔드 API 설계 (초안)

> 경로와 응답 형태는 제안이며, 공공 API 명세 확인 후 조정한다.
> 토지대장·토지이용계획은 `pnu`로, 건축물대장은 `sigunguCd`+`bjdongCd`+`platGbCd`+`bun`+`ji` 5개 값으로 식별한다(8-1 참조) — 식별 방식이 서로 달라 `{필지식별자}`는 표기상 편의이며, 실제로는 백엔드가 도로명주소 API 응답(`admCd`/`mtYn`/`lnbrMnnm`/`lnbrSlno`)에서 두 형태를 모두 파생시켜야 한다. `admCd`가 정확히 10자리로 나뉘는지는 8-2장 1번 참조(실제 호출로 확인 필요).

| 메서드 | 경로 | 설명 |
| --- | --- | --- |
| GET | `/api/address/search?q=` | 주소 검색, 후보 목록 반환 |
| GET | `/api/land/{필지식별자}` | 토지대장 |
| GET | `/api/building/{필지식별자}` | 건축물대장 |
| GET | `/api/land-price/{필지식별자}` | 공시지가 5개년 |
| GET | `/api/land-use/{필지식별자}` | 토지이용계획 |
| GET | `/api/coordinates?address=` | 지도 표시용 좌표 (VWorld Geocoder 경유) |

### 공통 규약

- 모든 응답은 동일한 봉투(envelope) 구조로 통일
- 데이터 없음(정상)과 오류(비정상)를 HTTP 상태 코드로 구분
- 공공 API 오류 메시지를 그대로 노출하지 않고 서비스 표준 오류로 변환

---

## 10. 비기능 요구사항

| 항목 | 내용 |
| --- | --- |
| 응답 속도 | 매 조회마다 공공 API 호출 결과에 종속 (캐시 없음) |
| 타임아웃 | 공공 API 호출에 타임아웃 설정, 초과 시 오류 상태로 전환 |
| 오류 처리 | 4종 중 하나가 실패해도 나머지 pills는 정상 동작해야 함 |
| 호출량 | Geocoder API는 일 40,000건 한도 확인됨. 나머지 5개는 미확인 (캐시가 없어 제한에 더 민감함) |
| 반응형 | 데스크톱 기준, 모바일 대응 불필요 |

---

## 11. 미결정 사항

기획을 확정하려면 아래 항목에 대한 결정 또는 확인이 필요하다.

| # | 항목 | 성격 | 비고 |
| --- | --- | --- | --- |
| 1 | 도로명주소 `admCd`가 10자리 법정동코드로 정확히 나뉘는지 | 확인 필요 | 8-2장 1번 — **최우선**. 실제 호출 테스트로만 확인 가능 |
| 2 | F-05 건폐율·용적률·규제를 어느 API/데이터로 얻을지 | 건폐율과 용적률은 건축물대장 api 얻음. 규제는 기획에서 제외함 | 8-2장 2번 — `getLandUseAttr`엔 없음이 **확정됨**, 별도 출처 조사 필요 |
| 3 | 개별공시지가 필지 단위 조회 방법 및 5개년 조회 방식 | 년도별 조회 | 8-2장 3번 |
| 4 | 도로명주소 · 지번주소 입력 모두 지원 여부 | 도로명 주소만 지원 | 검색 API 지원 범위에 종속 (1번 확인 후 결정) |
| 5 | 차트 라이브러리 선정 | Recharts | |
| 6 | 지도 표시 라이브러리 선정 | Leaflet** (`react-leaflet`) | 좌표 획득(Geocoder)은 확인됨 — 지도를 실제로 그릴 라이브러리/위젯만 미정 |

### 결정된 사항 (참고)

| 항목 | 결정 내용 |
| --- | --- |
| 6개 API 인증키 발급 단위 | 도로명주소 포함 5개는 `VWORLD_API_KEY` 통합, 건축물대장은 `BUILDING_API_KEY` 별도 (확정, 2026-07-27) |
| 도로명주소·건축물대장 API 명세 | Chrome DevTools로 실제 문서 페이지를 렌더링해 확인 완료 (2026-07-27) — 8-1 참조 |
| 배포 환경 | 로컬 |

---

## 12. 개발 단계

> **구현 현황 (2026-07-28)** — `frontend/`에 목업 데이터 기반 UI가 이미 구현되어 있다: 필지 탭바, 검색/후보 목록, pills 앵커 내비게이션, 요약·토지이용계획·토지대장·건축물대장·공시지가 5개 섹션(각 섹션의 로딩/정상/데이터없음/오류 상태 포함), 공시지가 SVG 차트(결측 연도 처리 포함), 목업 지도. 즉 Phase 1~4의 **프론트엔드 UI 부분은 목업 기준으로 완료**됐고, 남은 건 실제 백엔드(FastAPI)와 공공 API 연동이다. React는 TypeScript 없이 순수 JS(JSX)로, 스타일은 별도 프레임워크 없이 인라인 스타일로 작성됨.

### Phase 0 — 사전 확인 (선행 필수)

- 공공 API 인증키 발급: `VWORLD_API_KEY`(도로명주소·토지임야정보·토지이용계획·개별공시지가·Geocoder 5종 공용), `BUILDING_API_KEY`(건축물대장)
- 발급받은 키로 6개 API 실제 호출 테스트 (문서 확인은 완료됨, 8-1 참조)
- 11장 1~3번 항목 확인 → 확인 결과에 따라 8 · 9장 확정
- 나머지 미결정 사항 결정

> Phase 0 결과에 따라 이후 설계가 바뀔 수 있으므로, 완료 전까지 본격 구현에 들어가지 않는다.

### Phase 1 — 기반 구축

- FastAPI 프로젝트 골격, React + Vite 프로젝트 골격
- 공통 응답 규약 및 오류 처리 구조
- 공공 API Client 레이어 뼈대

### Phase 2 — 주소 검색

- F-01 구현 (백엔드 검색 엔드포인트 + 프론트 검색 / 목록 선택 UI)
- 필지 선택 후 상태 관리 흐름 확립

### Phase 3 — 4종 조회

- F-05 토지이용계획
- F-02 토지대장
- F-03 건축물대장
- F-04 공시지가 (데이터 조회까지)
- 보고서 레이아웃(섹션 순서 배치) 및 앵커 내비게이션(pills), 섹션별 독립 상태 처리

### Phase 4 — 차트 · 지도

- F-04 꺾은선 차트
- F-04 지도 UI (좌: 지도, 우: 차트 배치)

### Phase 5 — 마감

- 오류 / 예외 상황 점검
- 배포
